import React from 'react';
import {
    createBrowserRouter,
    RouterProvider,
} from "react-router";

const Router = () => {
    return (
        <div>

        </div>
    );
};

export default Router;